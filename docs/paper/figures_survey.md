# 그림 자산 조사 (사전 작업 — Task 3)

## A. 바로 쓸 수 있는 실제 자산

| 파일 | 내용 | 촬영/생성 시점 | 보고서 활용처(제안) |
|---|---|---|---|
| `docs/screenshots/dashboard.png` | 웹앱 상황판(KPI·히트맵·덤벨차트) | 2026-07-08 Playwright | 7장 웹 서비스 구현 |
| `docs/screenshots/chat.png` | 조사 채팅 화면(EV6 시나리오, 5단계 완료 후 답변) | 2026-07-08 Playwright | 7장 |
| `docs/screenshots/mycar.png` | 내 차 3D/SVG 도메인 지도 | 2026-07-08 Playwright | 7장 |

3장 전부 `README.md`에도 이미 삽입돼 있어 최신 화면과 다소 어긋날 수 있음(이후 세션에서 UI가 여러 차례 개편됨 — 7.6~7.17단계) — 보고서용으로 재촬영할지 이 3장을 그대로 쓸지 다음 턴에 결정 필요.

## B. 존재하지만 정식 자산 아님 (재검토 필요)

| 파일 | 내용 | 비고 |
|---|---|---|
| `.playwright-mcp/page-2026-07-16T16-44-30-969Z.png` 외 5장 | 3D 핫스팟 보정 세션(7.17단계)의 디버그 스크린샷 | `.gitignore`에 걸려 있어 추적 안 됨(휘발성). 필요하면 재촬영 권장 — 현재 화질·구도가 보고서용으로 다듬어지지 않음 |

## C. 없어서 TikZ/표로 대체해야 하는 그림

| 필요한 그림 | 해당 절 | 대체 방안 |
|---|---|---|
| 파이프라인 6단계 다이어그램(감지→구조화→조사→판정→검수→리포트) | 4장 | TikZ 플로우차트(박스+화살표), 각 박스에 실제 스크립트 파일명 라벨 |
| 모델 라우팅(Hybrid) 도식 | 4장 | TikZ 또는 표(단계 × 모델 2열) |
| 좋은 Agent 6기준 대응표 | 2장 | 표(기준 × 본 프로젝트 구현 근거) — 단, 6기준 자체가 [확인 필요] 상태라 표 골격만 우선 확정 |
| 데이터 파이프라인(수집→필터→정답지 분리) 개념도 | 3장 | TikZ 박스 다이어그램 |
| B1 감지 baseline 시계열 예시(EV9 스파이크) | 5.1절 | `data/processed/report_24V757000.md`에 서술은 있으나 차트 이미지 없음 — matplotlib로 새로 그리거나 표로 대체 |
| K12 되감기 결과 요약 | 5장 | `b1_eval_k12.csv`를 표로 직접 삽입(그림 불필요, 표로 충분) |
| Self-Consistency 안정성 분포 | 6장 | `str03_consistency_v4_fixed_detail.csv` 기반 막대그래프 신규 생성 필요(현재 마크다운 표만 존재) |
| 아키텍처 다이어그램(webapp 백엔드/프론트엔드 구조) | 7장 | TikZ 박스 다이어그램. CLAUDE.md의 라우터/컴포넌트 구조 서술을 근거로 작성 가능 |

## D. 웹앱 소스 내 비-사진 자산 (참고용, 직접 삽입 대상 아님)

- `webapp/frontend/src/assets/logo_mobiscope.png` — 로고. 표지/타이틀에 쓸지는 다음 턴 판단
- `webapp/frontend/public/models/{suv,sedan,sports}.glb` — 3D 모델 원본(이미지 아님, 스크린샷으로만 활용 가능)
- `webapp/frontend/public/{favicon.svg,icons.svg}` — UI 아이콘, 보고서에 부적합

## 결론
현재 확보된 실사진은 3장(A)뿐. 4·5·6장에 필요한 다이어그램/차트는 전부 신규 제작(TikZ 또는 matplotlib) 필요 — 본문 작성 단계에서 절별로 진행.
